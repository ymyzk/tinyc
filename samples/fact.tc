int fact(int n) {
    int i, result;
    i = 0;
    result = 1;
    while (++i <= n) {
        result = result * i;
    }
    return result;
}

int main() {
    test(fact(0), 1);
    test(fact(1), 1);
    test(fact(2), 2);
    test(fact(10), 3628800);
    return 0;
}
